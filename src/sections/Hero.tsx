import React, { useEffect, useState } from 'react'

export default function Hero(){
    return(
        <article className='container'>
            <h1 className="text-4xl font-bold text-red-500">Pagina en desarollo, en dias estará terminada.</h1>
        </article>
    );
};